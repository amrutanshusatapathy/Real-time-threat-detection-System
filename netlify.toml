# Netlify config for deploying the React/Vite frontend

[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing (Vite/React)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
